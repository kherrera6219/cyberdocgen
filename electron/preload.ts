// Preload script for Electron
// Currently empty but reserved for secure IPC between renderer and main process
window.addEventListener('DOMContentLoaded', () => {
  console.log('CyberDocGen Electron Wrapper Loaded');
});
